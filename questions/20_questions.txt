### **Easy: Fundamentals & Definitions (8 Questions)**
These questions test basic recall and high-level understanding of what the protocol is and its primary components.

1. **What does the acronym "MCP" stand for in the context of AI engineering?**
2. **What is the primary problem that the Model Context Protocol was created to solve?**
3. **In the MCP architecture, what is the role of the "Client" (e.g., Claude Desktop or your custom IDE)?**
4. **In the MCP architecture, what is the role of an "MCP Server"?**
5. **Name the two primary transport mechanisms used by MCP to establish a connection.**
6. **True or False: The Model Context Protocol requires all servers to be written in TypeScript.**
7. **What is the definition of an MCP "Resource"?**
8. **What is the definition of an MCP "Tool"?**

---

### **Medium: Processes & Architecture (8 Questions)**
These questions require the student to understand how the components interact, the data structures used, and the differences between core features.

9. **Explain the functional difference between exposing data as an MCP "Resource" versus exposing it via an MCP "Tool".**
10. **Describe the client-server handshake process. What happens when an MCP connection is first initialized?**
11. **How do MCP "Prompts" help standardize user interactions with an AI model?**
12. **Compare the `stdio` transport and the `SSE` (Server-Sent Events) transport. In what scenario would a developer choose one over the other?**
13. **Under the hood, what messaging protocol does MCP use to format the data being sent back and forth?**
14. **If a user wants an AI to be able to read a static local configuration file, which MCP feature (Resource, Tool, or Prompt) should the server developer use, and why?**
15. **If a user wants an AI to be able to write or update a database record, which MCP feature should be used, and why?**
16. **How does the host application (the client) discover which Tools and Resources a connected MCP server actually supports?**

---

### **Hard: Security, Implementation & Edge Cases (4 Questions)**
These questions require synthesizing concepts, understanding security implications, and designing workflows.

17. **How does MCP's user-controlled, opt-in authorization model mitigate the security risks of granting a Large Language Model access to a local filesystem?**
18. **Detail the exact sequence of events and messages exchanged when an LLM decides to execute an MCP Tool, from the initial model generation to receiving the final tool execution result.**
19. **Discuss the security implications and potential vulnerabilities of exposing an MCP server over a network using SSE, compared to running it purely locally via stdio.**
20. **If you were building a custom MCP server to connect an LLM to a rate-limited external API (like Shopify or GitHub), how would you implement rate limiting or error handling within the server to prevent the AI from spamming the API?**